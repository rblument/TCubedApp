<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>
    <groupId>edu.regis</groupId>
    <artifactId>TCubedApp</artifactId>
    <version>1.0-SNAPSHOT</version>
 
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
    <!-- Forces Maven to search the central internet repository instead of looking upstairs -->
    <relativePath/> 
  </parent> 

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
    </dependency>
  </dependencies>

  <!-- Profiles dynamically adjust settings based on the developer's Operating System -->
  <profiles>
    <profile>
      <id>windows-onedrive-fix</id>
      <activation>
        <os>
          <family>windows</family>
        </os>
      </activation>
      <build>
        <!-- This directory avoids locking problems on OneDrive Cloud file systems for Windows users -->
        <directory>C:/NetBeansBuilds/${project.artifactId}/target</directory>
      </build>
    </profile>
  </profiles>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
        <!-- Explicitly declares the exact entry point class for Maven -->
        <mainClass>edu.regis.tcubed.TCubedApp</mainClass>
      </configuration>
      </plugin>
    </plugins>
  </build>
</project>